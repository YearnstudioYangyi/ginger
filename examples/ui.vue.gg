模板
    盒子
        标题，内容为Application
        文字，内容为count的值
    按钮
脚本
从vue导入ref
定义一个常量名为count，值为调用ref的返回值，调用时传入参数0
声明一个名为increase的函数，实现如下
    将count的value的值加1
样式
    盒子
        宽度，值为100%
        高度，值为100%
        背景色，值为#f0f0f0
        居中，值为true
    标题
        字体大小，值为48px
        字体粗细，值为bold
    文字
        字体大小，值为48px
        字体粗细，值为bold
    按钮
        宽度，值为100%
        高度，值为48px
        背景色，值为#007bff
        字体颜色，值为#ffffff