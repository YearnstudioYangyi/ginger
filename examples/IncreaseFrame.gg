模板
    盒子
        文字，内容为 数字 的值
        按钮，绑定click事件的处理函数为 增加

下面是脚本标签，使用vue3的setup语法糖，设置lang属性为ts
从vue导入ref

定义一个常量名为数字，初始值为“ref”函数的返回值，调用函数时传入数字0

声明一个函数名为增加，实现如下：
    把数字的value字段增加1

样式，设置scoped为true
盒子
    宽度200像素，
    高度100像素，
    使用弹性盒布局，
    垂直居中，
    水平居中，
    边框1像素，灰色，实线，
    圆角5像素，
    弹性盒方向设为纵向